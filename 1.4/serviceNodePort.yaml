apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
  - name: nginx
    protocol: TCP
    port: 9001
    targetPort: 80
    nodePort: 30400
  - name: http-port
    port: 9002
    protocol: TCP
    targetPort: 8080
    nodePort: 30500
